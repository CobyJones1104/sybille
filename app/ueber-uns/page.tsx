import type { Metadata } from "next";
import { ComingSoon } from "@/components/layout/coming-soon";

export const metadata: Metadata = { title: "Über uns / Der Laden – Sybille's Nähparadies" };

export default function UeberUnsPage() {
  return <ComingSoon title="Über uns / Der Laden" />;
}
